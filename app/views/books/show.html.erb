<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>Show</title>
  </head>

  <body>
    <p><strong>Title:</strong>
      <%= @book.title %>
    </p>
    <p>
      <strong>Body:</strong>
      <%= @book.body %>
    </p>
    <%= link_to "Edit |", "/books/#{@book.id}/edit" %>
    <%= link_to "Back", "/books" %>
  </body>
</html>
